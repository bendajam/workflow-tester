name: Deploy to QA

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to qa
    runs-on: ubuntu-latest
    environment: qa
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: echo "Building ${{ github.ref }} (${{ github.sha }}) for qa"

      - name: Deploy to qa
        run: echo "Deploying ${{ github.ref }} (${{ github.sha }}) to qa environment"
